services:
  # Serviço para o seu site Next.js
  web:
    build:
      context: .
      dockerfile: Dockerfile.next
    restart: "always"
    ports:
      - "0.0.0.0:8080:3000"
    volumes:
      # NOVO VOLUME: VM /teste/app/data para Container /app/data
      - /teste/app/data:/app/data
